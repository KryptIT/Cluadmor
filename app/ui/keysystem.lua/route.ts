import { looksLikeBrowserNavigation, protectedHtml } from "@/lib/protected-view";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const SOURCE = "local Library = {\n    service = nil,\n    identifier = nil,\n    provider = nil\n}\n\nlocal function env()\n    local result = type(_G) == \"table\" and _G or {}\n    if type(getgenv) == \"function\" then\n        local ok, value = pcall(getgenv)\n        if ok and type(value) == \"table\" then\n            result = value\n        end\n    end\n    return result\nend\n\nlocal function cleanId(value)\n    return tostring(value or \"default\"):gsub(\"[^%w_%-]\", \"_\")\nend\n\nlocal function keyFile(serviceId)\n    return \"claudmor_key_\" .. cleanId(serviceId or Library.service) .. \".txt\"\nend\n\nlocal function fileSupport()\n    return type(writefile) == \"function\" and type(readfile) == \"function\"\nend\n\nfunction Library.setKey(value)\n    local key = tostring(value or \"\")\n    env().SCRIPT_KEY = key\n    return key\nend\n\nfunction Library.getKey()\n    local value = env().SCRIPT_KEY\n    return type(value) == \"string\" and value or \"\"\nend\n\nfunction Library.saveKey(serviceId, value)\n    local key = tostring(value or \"\")\n    if key == \"\" or not fileSupport() then return false end\n\n    local ok = pcall(function()\n        writefile(keyFile(serviceId), key)\n    end)\n\n    return ok\nend\n\nfunction Library.loadSavedKey(serviceId)\n    if not fileSupport() then return nil end\n\n    local ok, value = pcall(function()\n        return readfile(keyFile(serviceId))\n    end)\n\n    if not ok or type(value) ~= \"string\" then\n        return nil\n    end\n\n    value = value:match(\"^%s*(.-)%s*$\")\n    return value ~= \"\" and value or nil\nend\n\nfunction Library.clearSavedKey(serviceId)\n    if type(delfile) ~= \"function\" then return false end\n\n    local ok = pcall(function()\n        delfile(keyFile(serviceId))\n    end)\n\n    return ok\nend\n\nlocal function parentGui()\n    if type(gethui) == \"function\" then\n        local ok, value = pcall(gethui)\n        if ok and value then return value end\n    end\n\n    local ok, core = pcall(function()\n        return game:GetService(\"CoreGui\")\n    end)\n    if ok and core then return core end\n\n    local players = game:GetService(\"Players\")\n    local player = players.LocalPlayer\n    return player and player:FindFirstChildOfClass(\"PlayerGui\")\nend\n\nfunction Library.prompt(options)\n    options = type(options) == \"table\" and options or {}\n\n    local current = Library.getKey()\n\n    local UIS = game:GetService(\"UserInputService\")\n    local done = Instance.new(\"BindableEvent\")\n    local gui = Instance.new(\"ScreenGui\")\n\n    gui.Name = \"ClaudmorKeyUI\"\n    gui.ResetOnSpawn = false\n    gui.IgnoreGuiInset = true\n    gui.DisplayOrder = 999999\n    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling\n    gui.Parent = parentGui()\n\n    local backdrop = Instance.new(\"Frame\")\n    backdrop.Size = UDim2.fromScale(1, 1)\n    backdrop.BackgroundColor3 = Color3.fromRGB(0, 0, 0)\n    backdrop.BackgroundTransparency = 0.28\n    backdrop.BorderSizePixel = 0\n    backdrop.Parent = gui\n\n    local frame = Instance.new(\"Frame\")\n    frame.Name = \"Window\"\n    frame.AnchorPoint = Vector2.new(0.5, 0.5)\n    frame.Position = UDim2.fromScale(0.5, 0.5)\n    frame.Size = UDim2.fromOffset(430, 248)\n    frame.BackgroundColor3 = Color3.fromRGB(8, 12, 14)\n    frame.BorderSizePixel = 0\n    frame.Parent = backdrop\n\n    if UIS.TouchEnabled and not UIS.KeyboardEnabled then\n        frame.Size = UDim2.new(0.88, 0, 0, 248)\n    end\n\n    local corner = Instance.new(\"UICorner\")\n    corner.CornerRadius = UDim.new(0, 10)\n    corner.Parent = frame\n\n    local stroke = Instance.new(\"UIStroke\")\n    stroke.Color = Color3.fromRGB(28, 59, 66)\n    stroke.Thickness = 1\n    stroke.Parent = frame\n\n    local header = Instance.new(\"Frame\")\n    header.Size = UDim2.new(1, 0, 0, 63)\n    header.BackgroundTransparency = 1\n    header.Parent = frame\n\n    local title = Instance.new(\"TextLabel\")\n    title.BackgroundTransparency = 1\n    title.Position = UDim2.fromOffset(18, 11)\n    title.Size = UDim2.new(1, -36, 0, 24)\n    title.Font = Enum.Font.GothamBold\n    title.TextSize = 18\n    title.TextColor3 = Color3.fromRGB(235, 244, 246)\n    title.TextXAlignment = Enum.TextXAlignment.Left\n    title.Text = tostring(options.title or \"Claudmor\")\n    title.Parent = header\n\n    local subtitle = Instance.new(\"TextLabel\")\n    subtitle.BackgroundTransparency = 1\n    subtitle.Position = UDim2.fromOffset(18, 36)\n    subtitle.Size = UDim2.new(1, -36, 0, 18)\n    subtitle.Font = Enum.Font.Gotham\n    subtitle.TextSize = 10\n    subtitle.TextColor3 = Color3.fromRGB(105, 121, 128)\n    subtitle.TextXAlignment = Enum.TextXAlignment.Left\n    subtitle.Text = tostring(options.description or \"Enter your access key to continue.\")\n    subtitle.Parent = header\n\n    local box = Instance.new(\"TextBox\")\n    box.Position = UDim2.fromOffset(18, 75)\n    box.Size = UDim2.new(1, -36, 0, 44)\n    box.BackgroundColor3 = Color3.fromRGB(5, 9, 11)\n    box.BorderSizePixel = 0\n    box.ClearTextOnFocus = false\n    box.Font = Enum.Font.Code\n    box.TextSize = 13\n    box.TextColor3 = Color3.fromRGB(218, 234, 237)\n    box.PlaceholderColor3 = Color3.fromRGB(67, 82, 88)\n    box.PlaceholderText = tostring(options.placeholder or \"Enter access key\")\n    local initialValue = options.initialValue\n    if type(initialValue) ~= \"string\" or initialValue == \"\" then\n        initialValue = current\n    end\n    box.Text = type(initialValue) == \"string\" and initialValue or \"\"\n    box.Parent = frame\n\n    local boxCorner = Instance.new(\"UICorner\")\n    boxCorner.CornerRadius = UDim.new(0, 6)\n    boxCorner.Parent = box\n\n    local boxStroke = Instance.new(\"UIStroke\")\n    boxStroke.Color = Color3.fromRGB(28, 49, 55)\n    boxStroke.Parent = box\n\n    local submit = Instance.new(\"TextButton\")\n    submit.Position = UDim2.fromOffset(18, 133)\n    submit.Size = UDim2.new(1, -36, 0, 42)\n    submit.BackgroundColor3 = Color3.fromRGB(18, 88, 101)\n    submit.BorderSizePixel = 0\n    submit.AutoButtonColor = false\n    submit.Font = Enum.Font.GothamBold\n    submit.TextSize = 12\n    submit.TextColor3 = Color3.fromRGB(230, 250, 252)\n    submit.Text = tostring(options.submitText or \"Verify key\")\n    submit.Parent = frame\n\n    local submitCorner = Instance.new(\"UICorner\")\n    submitCorner.CornerRadius = UDim.new(0, 6)\n    submitCorner.Parent = submit\n\n    local status = Instance.new(\"TextLabel\")\n    status.BackgroundTransparency = 1\n    status.Position = UDim2.fromOffset(18, 187)\n    status.Size = UDim2.new(1, -36, 0, 36)\n    status.Font = Enum.Font.Gotham\n    status.TextSize = 10\n    status.TextWrapped = true\n    status.TextColor3 = Color3.fromRGB(90, 108, 114)\n    status.TextXAlignment = Enum.TextXAlignment.Left\n    status.TextYAlignment = Enum.TextYAlignment.Top\n    status.Text = tostring(options.errorText or \"Protected by Claudmor\")\n    status.Parent = frame\n\n    if options.errorText then\n        status.TextColor3 = Color3.fromRGB(215, 111, 111)\n    end\n\n    local dragging = false\n    local dragStart = nil\n    local startPos = nil\n\n    header.InputBegan:Connect(function(input)\n        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then\n            dragging = true\n            dragStart = input.Position\n            startPos = frame.Position\n        end\n    end)\n\n    UIS.InputChanged:Connect(function(input)\n        if dragging and dragStart and startPos and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then\n            local delta = input.Position - dragStart\n            frame.Position = UDim2.new(\n                startPos.X.Scale,\n                startPos.X.Offset + delta.X,\n                startPos.Y.Scale,\n                startPos.Y.Offset + delta.Y\n            )\n        end\n    end)\n\n    UIS.InputEnded:Connect(function(input)\n        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then\n            dragging = false\n        end\n    end)\n\n    local submitted = false\n\n    local function submitKey()\n        if submitted then return end\n\n        local value = box.Text:match(\"^%s*(.-)%s*$\")\n        if value == \"\" then\n            status.Text = \"Enter a key first.\"\n            status.TextColor3 = Color3.fromRGB(215, 111, 111)\n            return\n        end\n\n        submitted = true\n        submit.Text = \"Checking...\"\n        submit.Active = false\n\n        Library.setKey(value)\n\n        if options.remember ~= false then\n            Library.saveKey(options.serviceId or Library.service, value)\n        end\n\n        done:Fire(value)\n    end\n\n    submit.MouseButton1Click:Connect(submitKey)\n    box.FocusLost:Connect(function(enterPressed)\n        if enterPressed then submitKey() end\n    end)\n\n    box:CaptureFocus()\n\n    local value = done.Event:Wait()\n    done:Destroy()\n    gui:Destroy()\n\n    return value\nend\n\nfunction Library.run(loaderUrl, key)\n    if key ~= nil then\n        Library.setKey(key)\n    end\n\n    local compiler = loadstring or load\n    assert(type(compiler) == \"function\", \"loadstring unavailable\")\n\n    local chunk, err = compiler(game:HttpGet(tostring(loaderUrl)), \"@Claudmor/run\")\n    assert(chunk, err)\n\n    return chunk()\nend\n\nreturn Library";

export async function GET(req: Request) {
  if (looksLikeBrowserNavigation(req)) {
    return new Response(protectedHtml("source"), {
      status: 403,
      headers: {
        "Content-Type": "text/html; charset=utf-8",
        "Cache-Control": "no-store, max-age=0",
        "X-Content-Type-Options": "nosniff",
        "X-Robots-Tag": "noindex, nofollow, noarchive",
        "Content-Security-Policy": "default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'"
      }
    });
  }

  return new Response(SOURCE, {
    status: 200,
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, max-age=120",
      "X-Content-Type-Options": "nosniff"
    }
  });
}
